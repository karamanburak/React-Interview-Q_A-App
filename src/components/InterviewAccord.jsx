import {data} from '../helpers/data'

const InterviewAccord = ({answer}) => {
    console.log(answer);
 

  return (
          <div>
            <p>{answer}</p>
          </div>
                            
  )
}

export default InterviewAccord

